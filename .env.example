REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

LEADERBOARD_PREFIX=leaderboard
TOP_N=10
USER_NEIGHBORHOOD=4

DB_DSN=root:password@tcp(localhost:3306)/leaderboard?parseTime=true

BATCH_FLUSH_MS=100
BATCH_SIZE=500

# Shared secret with the upstream game service.
# Set to a non-empty value in production; leave empty to disable auth (local dev).
INTERNAL_API_KEY=
